apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: step-issuer-operator
  namespace: flux-system
spec:
  interval: 10m
  path: ./infra/step-issuer/operator
  prune: true
  wait: true
  dependsOn:
    - name: cert-manager-operator
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
