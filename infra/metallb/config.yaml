apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: metallb-config
  namespace: flux-system
spec:
  interval: 10m
  path: ./infra/metallb/config
  prune: true
  dependsOn:
    - name: metallb-base
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system