apiVersion: v1
kind: Service
metadata:
  name: step-ca-frontend
  namespace: step-ca
  labels:
    app.kubernetes.io/name: step-ca
    app.kubernetes.io/instance: step-ca-frontend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: step-ca
    app.kubernetes.io/instance: step-ca-frontend
  ports:
    - name: https
      port: 9000
      targetPort: https
      protocol: TCP
