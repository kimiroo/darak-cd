apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: step-ca

resources:
  - service.yaml
  - statefulset.yaml

configMapGenerator:
  - name: step-ca-frontend-certs
    files:
      - root_ca.crt
      - intermediate_ca.crt
  - name: step-ca-frontend-ca-json
    files:
      - ca.json