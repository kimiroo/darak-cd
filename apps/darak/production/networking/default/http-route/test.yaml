apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: darak-prod-test
  namespace: darak-prod
spec:
  parentRefs:
  - name: darak-prod
    namespace: darak-prod
  hostnames:
  - "test.darak.dev"
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs: []
