apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: darak-prod

resources:
  - certificate.yaml
  - gateway.yaml
  - backend-client-certificate.yaml
  - client-traffic-policy.yaml
  - http-route

configMapGenerator:
  - name: darak-prod-mtls-client-ca
    files:
      - ca.crt

configurations:
  - kustomizeconfig.yaml
