apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cert-manager-issuer
  namespace: cert-manager
spec:
  interval: 10m
  path: ./clusters/ichika-cluster/infra/cert-manager/issuer
  prune: true
  dependsOn:
    - name: cert-manager-substitude
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system